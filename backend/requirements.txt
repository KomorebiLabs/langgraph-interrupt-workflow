# Core framework — LangGraph & LangChain v1 (stable; no breaking changes until 2.0)
langgraph>=1.2,<2
langchain>=1.3,<2
langchain-core>=1.4,<2

# Durable, resumable checkpointing
langgraph-checkpoint-sqlite>=3.1,<4
aiosqlite>=0.20

# Deep Agent engine (planning + subagents). Optional — the app boots without it,
# with the Deep Agent engine disabled.
deepagents>=0.6,<1

# AG-UI protocol adapter — exposes the agent to any AG-UI client (CopilotKit,
# etc.) at /agui. Optional — the app boots without it (endpoint just not mounted).
ag-ui-langgraph>=0.0.40

# API server
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
python-multipart>=0.0.12
python-dotenv>=1.0

# --- LLM providers (install the one you use) ---
# The template is provider-agnostic via langchain's init_chat_model.
langchain-openai>=1.2,<2
# langchain-anthropic>=1.0
# langchain-google-genai>=2.0
# langchain-ibm>=0.3            # IBM watsonx (set LLM_PROVIDER=ibm)

# --- Optional tools ---
# langchain-tavily>=0.2         # live web search (set TAVILY_API_KEY)
# langchain-mcp-adapters>=0.1   # load tools from MCP servers (set MCP_SERVERS)

# --- Evaluation (backend/evals) ---
# Pulled in transitively by langchain; pinned here for the eval harness.
langsmith>=0.9,<1
